# Implementation Artifact: E11.4 Code Validation (ESLint + tsc)

## Story Details
- **Story Identifier**: `E11.4`
- **Epic**: Epic 11 — Playwright Test Generation & Validation
- **User Story**: As the **Platform**, I want generated code validated, so that compile rate $\ge 95\%$.
- **Status**: Implemented & Verified
- **Date**: 2026-08-27

## Acceptance Criteria Summary
- **AC1: Given generated files When validator runs Then ESLint custom rules pass (`no-xpath` in spec, PO encapsulation)**:
  - Implemented `CodeValidator` in [`packages/shared/src/generation/code-validator.ts`](file:///c:/Users/abch6/Desktop/QA%20Automater/packages/shared/src/generation/code-validator.ts).
  - `no-xpath`: Scans generated `.ts` code and flags errors if XPath locator patterns (`xpath=`, `//div`, etc.) are detected.
  - `po-encapsulation`: Verifies that test spec files (`*.spec.ts`) do not invoke raw `page.locator`, `page.getByTestId`, or `page.getByRole` directly in the test body.
- **AC2: Given TypeScript output When `tsc --noEmit` / AST Compiler runs Then zero syntax errors or job marked failed with diagnostics**:
  - Uses TypeScript Compiler AST Parser (`ts.createSourceFile`) to validate syntax correctness, returning line numbers, column numbers, and error message strings.

## Verification Results
- **Shared Package Unit Tests**: `pnpm --filter @qa-automater/shared test` — Passed 100% (92/92 tests passed, including 5 new test cases in `code-validator.test.ts`).
- **Prettier Code Style**: `pnpm format` & `pnpm format:check` — 100% matched style guidelines.
- **Monorepo Lint**: `pnpm lint` — 13/13 turbo packages passed with 0 errors.
