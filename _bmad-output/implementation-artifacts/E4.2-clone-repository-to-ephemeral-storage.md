# E4.2 — Clone Repository to Ephemeral Storage

**Status:** done  
**Date:** 2026-08-13  
**Epic:** E4 — Repository Scanner & Job Orchestration  

---

## User Story

As the **Scan Worker**, I want to shallow-clone repos, so that source is available for parsing.

---

## Acceptance Criteria

- **AC1:** Given valid token, when clone runs, then depth=1 shallow clone succeeds.
- **AC2:** Given clone complete, when tarball created, then uploaded to S3 at `{org_id}/{repo_id}/{commit}.tar.gz`.

---

## Technical Tasks

- [x] **Task 1:** Create `RepositoryCloner` service (`workers/scan/src/repository-cloner.ts`) handling depth=1 shallow clone with token authentication and commit hash resolution.
- [x] **Task 2:** Add tarball creation (`createTarball`) and S3 upload functionality in `RepositoryCloner` targeting key format `{org_id}/{repo_id}/{commit}.tar.gz` using `S3StorageService`.
- [x] **Task 3:** Update `workers/scan/src/index.ts` to wire up BullMQ `createWorker` processing `QueueName.SCAN` jobs using `RepositoryCloner`.
- [x] **Task 4:** Add unit tests in `workers/scan/src/repository-cloner.test.ts` verifying AC1 & AC2.

---

## Dev Agent Record

### Implementation Plan
1. Implemented `RepositoryCloner` in `workers/scan/src/repository-cloner.ts`:
   - Performs depth=1 shallow git clone to ephemeral temp directory.
   - Resolves commit hash via `git rev-parse HEAD`.
   - Packages repository into `.tar.gz` tarball archive.
   - Uploads tarball archive to S3 at `{org_id}/{repo_id}/{commit}.tar.gz`.
   - Performs ephemeral directory cleanup.
2. Updated worker task processor in `workers/scan/src/index.ts` listening to `QueueName.SCAN`.
3. Added unit test suite in `repository-cloner.test.ts`.

### Completion Notes
- All unit tests in `@qa-automater/scan-worker` passed 100%.
- TypeScript static analysis passed with zero errors (`pnpm typecheck`).
- ESLint passed with zero warnings/errors (`pnpm lint`).
- **AC1 Verified**: `cloneRepository` shallow clones repository with `depth=1` and specified branch.
- **AC2 Verified**: Tarball generated and uploaded to S3 via `S3StorageService.uploadArtifact` at key `{org_id}/{repo_id}/{commit}.tar.gz`.
- **Code Review**: Clean adversarial code review — 0 security/logic defects found. Status updated to **`done`**.

---

## File List

- `workers/scan/src/repository-cloner.ts` [NEW]
- `workers/scan/src/repository-cloner.test.ts` [NEW]
- `workers/scan/src/index.ts` [UPDATE]
