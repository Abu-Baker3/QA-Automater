# E5.3 — Implement NextJsAppRouterParser

**Status:** done  
**Date:** 2026-08-15  
**Epic:** E5 — UI Parser & Framework Detection  

---

## User Story

As the **Platform**, I want to parse Next.js App Router pages, so that routes map to components.

---

## Acceptance Criteria

- **AC1:** Given `app/login/page.tsx`, when route extraction runs, then page route `"/login"` linked to component and JSX elements.
- **AC2:** Given route groups `(auth)`, when parsed, then routes resolved correctly per Next.js conventions (`"/login"` with parentheses stripped).

---

## Technical Tasks

- [x] **Task 1:** Export `AppRouterRoute` and `AppRouterParseResult` interfaces in `@qa-automater/types` (`packages/types/src/index.ts`).
- [x] **Task 2:** Create `NextJsAppRouterParser` class in `workers/scan/src/nextjs-app-router-parser.ts`.
- [x] **Task 3:** Implement `pathToRoute(filePath)` to map `app/page.tsx` $\rightarrow$ `"/"`, `app/login/page.tsx` $\rightarrow$ `"/login"`, strip route groups `(auth)` $\rightarrow$ `"/login"` (**AC1 & AC2**), handle dynamic routes `[id]`, and exclude private folders `_components`.
- [x] **Task 4:** Implement `parseRouteFile` and `parseRepositoryRoutes` integrating `ReactParser` for page JSX element and component extraction.
- [x] **Task 5:** Create unit test suite in `workers/scan/src/nextjs-app-router-parser.test.ts` verifying AC1 and AC2.

### Review Findings

- [x] [Review][Defer] Intercepting route `(.)` & `@slot` parallel route metadata tagging [`workers/scan/src/nextjs-app-router-parser.ts`] — deferred, pre-existing MVP design

---

## Dev Agent Record

### Implementation Plan
1. Exported `AppRouterRoute` and `AppRouterParseResult` from `packages/types/src/index.ts`.
2. Implemented `NextJsAppRouterParser` (`workers/scan/src/nextjs-app-router-parser.ts`):
   - Path-to-route algorithm checks `page.(tsx|jsx|ts|js)` files under `app/` or `src/app/`.
   - Strips parentheses folders (route groups like `(auth)`), ignores private folders `_components`.
   - Delegates component & JSX element extraction to `ReactParser`.
3. Created Unit Tests (`workers/scan/src/nextjs-app-router-parser.test.ts`):
   - AC1: `app/login/page.tsx` $\rightarrow$ `"/login"` route linked to extracted JSX elements & components.
   - AC2: Route groups `app/(auth)/login/page.tsx` $\rightarrow$ `"/login"`.
   - Dynamic routes (`/users/[id]`), root page (`/`), private folder exclusions (`_components`), and multi-file repository route aggregation.

### Completion Notes
- All 5 test files (25 tests) in `@qa-automater/scan-worker` passed 100%.
- All 23 test files (82 tests) in `@qa-automater/api` passed 100%.
- Prettier code style check passed cleanly (`pnpm format:check`).
- Monorepo Turbo pipeline passed 36/36 tasks cleanly (`pnpm exec turbo run build typecheck test lint`).
- **Code Review**: Clean adversarial code review — 0 logic/security defects. Status updated to **`done`**.

---

## File List

- `packages/types/src/index.ts` [UPDATE]
- `workers/scan/src/nextjs-app-router-parser.ts` [NEW]
- `workers/scan/src/nextjs-app-router-parser.test.ts` [NEW]
- `_bmad-output/implementation-artifacts/E5.3-implement-nextjs-app-router-parser.md` [NEW]
