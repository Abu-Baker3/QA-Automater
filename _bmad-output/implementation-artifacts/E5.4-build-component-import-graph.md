# E5.4 — Build Component Import Graph

**Status:** done  
**Date:** 2026-08-15  
**Epic:** E5 — UI Parser & Framework Detection  

---

## User Story

As the **Platform**, I want parent-child component relationships, so that UI hierarchy is accurate.

---

## Acceptance Criteria

- **AC1:** Given `Page` imports `LoginForm`, when graph built, then edge `page -> component -> child elements` exists.
- **AC2:** Given barrel re-export or circular import loop, when depth limit reached, then stop gracefully without crash.

---

## Technical Tasks

- [x] **Task 1:** Export `ComponentNode`, `ComponentEdge`, and `ComponentImportGraph` interfaces in `@qa-automater/types` (`packages/types/src/index.ts`).
- [x] **Task 2:** Create `ComponentImportGraphBuilder` class in `workers/scan/src/component-import-graph.ts`.
- [x] **Task 3:** Implement `parseImports` to extract default and named TypeScript import declarations using TypeScript AST.
- [x] **Task 4:** Implement `resolveImportPath` for relative (`./`, `../`) and path alias (`@/`) resolution.
- [x] **Task 5:** Implement `buildGraph` to link root route components to child components (`page -> component -> child elements`) (**AC1**), enforcing depth limit (`MAX_DEPTH = 10`) to prevent barrel re-export recursion crashes (**AC2**).
- [x] **Task 6:** Create unit test suite in `workers/scan/src/component-import-graph.test.ts` verifying AC1 and AC2.

### Review Findings

- [x] [Review][Defer] Dynamic `import()` expressions & React `lazy()` resolution [`workers/scan/src/component-import-graph.ts`] — deferred, pre-existing MVP design

---

## Dev Agent Record

### Implementation Plan
1. Exported `ComponentNode`, `ComponentEdge`, and `ComponentImportGraph` from `packages/types/src/index.ts`.
2. Implemented `ComponentImportGraphBuilder` (`workers/scan/src/component-import-graph.ts`):
   - Uses `ts.isImportDeclaration` to extract imports.
   - Resolves relative and path alias (`@/`) specifiers to actual repository file paths.
   - Discovers all component nodes and constructs parent-to-child edges (`page -> component -> child elements`).
   - Enforces depth limit (`MAX_DEPTH = 10`) to handle barrel re-exports and circular import loops without crashing.
3. Created Unit Tests (`workers/scan/src/component-import-graph.test.ts`):
   - AC1: `Page` imports `LoginForm`, `LoginForm` imports `Button` $\rightarrow$ parent-child edges exist. Path alias (`@/components/Header`) resolution.
   - AC2: Circular import loop with `maxDepth = 3` terminates gracefully without stack overflow.

### Completion Notes
- All 6 test files (28 tests) in `@qa-automater/scan-worker` passed 100%.
- All 23 test files (82 tests) in `@qa-automater/api` passed 100%.
- Prettier code style check passed cleanly (`pnpm format:check`).
- Monorepo Turbo pipeline passed 36/36 tasks cleanly (`pnpm exec turbo run build typecheck test lint`).
- **Code Review**: Clean adversarial code review — 0 logic/security defects. Status updated to **`done`**.

---

## File List

- `packages/types/src/index.ts` [UPDATE]
- `workers/scan/src/component-import-graph.ts` [NEW]
- `workers/scan/src/component-import-graph.test.ts` [NEW]
- `_bmad-output/implementation-artifacts/E5.4-build-component-import-graph.md` [NEW]
